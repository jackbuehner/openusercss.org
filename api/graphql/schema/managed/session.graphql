# import Managed from '../interface/managed.graphql'

type Session implements Managed {
  id: ID!
  createdAt: DateTime!
  updatedAt: DateTime!

  user:      User!
  token:     String!
  expiresAt: String!
  ip:        String!
  ua:        String!
}
