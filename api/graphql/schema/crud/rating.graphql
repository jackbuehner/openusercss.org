# import Crud from '../interface/crud.graphql'

type Rating implements Crud {
  id: ID!
  createdAt: DateTime!
  updatedAt: DateTime!
  createdBy: User!
  updatedBy: User!
  display:   String!

  theme: Theme!
  value: Int!
}

input RatingInput {
  theme: ID!
  value: Int!
}
